[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tuichat"
version = "0.1.0"
dependencies = ["typer", "textual", "rich"]

[project.scripts]
tuichat = "tuichat_client.cli:main"

